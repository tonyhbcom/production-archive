# 更新日志

## v1.1.1

**修：换了工作流之后，提示词读不出来**

- 症状：面板上那条「提示词」显示的是一串**模型文件名**
  （例如 `krea2_turbo_int8_convrot.safetensors`），真正的提示词不见了。
- 原因：识别提示词的旧策略是「**所有字符串里取最长的那个**」。
  换了工作流之后，**模型文件名比提示词长**（文件名 36 字 vs 提示词 35 字），它赢了。
  之前那套工作流的提示词几百字，所以这个坑一直没暴露。
- 现在改成三层判定：**先排除文件名字段与模型/媒体后缀 → 优先从文本节点里取 →
  实在没有就退到最长串**。而且最后那层兜底**也不会返回文件名** ——
  **宁可显示「无」，也不把一串文件名当成提示词**。
- 老用户的索引缓存会在升级后自动全量重建（缓存版本号已 +1，无需手动操作）。

## v1.1.0

**看得见**

- 列表里每条作品左边多了一张小预览图 —— 图片直接显示，视频取首帧；点它开大预览
- 缩略图「滚到哪加载到哪」，900 多条一起开也不卡
- 顶栏多了「**缩略图**」开关：不想要小图的关掉即可，列表更紧凑（选择会被记住）

**拉得动**

- 预览窗放大之后，按住画面就能拖着看细节了（上下左右随便拉）
  - 顺带修掉一个真 bug：以前用 CSS `transform: scale()` 只缩放、不改变布局尺寸，
    外层滚动条永远不会出现 —— 所以放大后被裁掉的部分根本拖不过去
  - 预览画面上的鼠标点击被主动掐断，不再冒泡出去。
    **如果你装了 comfyui-custom-scripts，它的图片灯箱是在 document 上监听图片点击的 ——
    以前一松手就会被它换成全屏大图。** 视频不受影响，因为那个灯箱只认 `img`
  - **拖动之后松手，不会再把窗口全屏／还原来回切了。**
    「点一下图片 = 全屏」是原设计，但**拖动收尾那一下也会被浏览器算作点击** ——
    于是你每拖一次，窗口就切换一次全屏，再拖再松又切回来。现在刚拖过的那一下会被放过。
  - 图片上的光标也抢了回来：那个灯箱给所有 `img` 设了 `cursor: zoom-in`，
    会盖掉我们的拖动光标
  - 拖拽时鼠标变成「抓紧」的手，松开回到「可抓」的手

**整理得动**

- **改名**：同一次生成的图 / 视频 / 音轨整组一起改，不会把一条作品拆散
- **移动到…**：移到输出目录里的任意位置，也可以当场新建子目录
- **删除**：软删除 —— 移进回收站，随时恢复，绝不真删
- **删除后一定会告诉你**：顶部弹一条「已移进回收站 xxx（顶部「回收站」可恢复）」，
  回收站面板开着的话还会自动刷新 —— 东西凭空从列表里消失，谁都会以为被真删了
- **批量删除**：勾选多条一次处理
- **回收站**：看清单、逐条恢复、彻底删除、一键清空。
  每条还会显示**提示词字数**，一眼看出提示词也跟着进了回收站，恢复就原样回来
- **回收站在哪，面板里写清楚了**：它**不是 Windows 系统回收站** —— 删除做的是「移动」
  而不是系统级删除，所以系统回收站里永远找不到它，别去那儿白找。
  面板直接显示真实路径，并多了一颗「**打开文件夹**」按钮，一点就在文件管理器里弹出来。
- **目录改名**：改真实文件夹名，目录规则自动跟着更新，归类不会丢

**安全**

- 所有写操作一律限制在输出目录之内，越界请求直接拒绝
- 绝不覆盖已存在的文件，重名自动加 `(2)`
- 删除后保留归类记录，恢复即自动接回原来的项目

**产品承诺的变化（重要）**

v1.0.0 的承诺是「只读，不改动、不移动、不删除任何输出文件」。
v1.1.0 起改为：**浏览 / 搜索 / 筛选 / 预览 / 归类全程只读；
只有你主动发起的整理动作才会写盘，且删除永远是软删除。**

## v1.0.0

首个公开版本。

**索引**

- 扫描输出目录里每个 PNG / MP4 内嵌的元数据，还原当时送进模型的完整提示词
- 同时记录 seed、分辨率、这次用到的参考图名单
- 增量扫描：只有新增或改动过的文件才会重新解析

**查找**

- 关键词搜索覆盖提示词正文、文件名、参考图名、seed、项目名
- 按项目、目录、类别（图片 / 视频）、时间区间筛选，条件之间取交集
- 项目、目录、类别都支持多选；目录另有「含子目录」开关
- 顶部常驻「全选 N 条」，N 随当前筛选结果实时变化

**归类**

- 自己建项目管理作品，可以改名、改色、删除
- 三种归类方式：单条指派、批量指派、目录规则（自动且对以后新出的作品持续生效）
- 优先级：手动归类 > 目录规则（目录路径更长者优先）> 未归类

**预览**

- 内置预览窗，图片和视频都能看
- 拖动标题栏移动窗口、拖右下角改大小、滚轮缩放、一键全屏
- 同一次输出里有多个文件时，视频优先使用带音轨的那一份

**其他**

- 只读：不改动、不移动、不删除任何输出文件
- 无第三方依赖，只用 Python 标准库
- 归类数据存在 ComfyUI 的 user 目录，升级或重装插件都不会丢

**修复**

- 首次打开面板时，数据其实已经取到了，但界面不刷新，显示成「还没有索引到任何记录」，
  要切到别的标签再切回来才有内容。原因是 ComfyUI 前端会把注册时交给我们那个容器换成新的，
  导致数据回来时的重绘找不到目标元素。

**关于名字**

包名（ComfyUI Registry 节点 ID）为 `production-archive`。

它归档的不只是提示词，而是整个输出目录里的生产成果——图片、视频、音频，
以及这些文件内嵌的提示词与工作流。名字取 `archive` 是想说清楚它到底干什么：
**把散落在输出目录里的历史作品收拢成一份可检索的档案。**
