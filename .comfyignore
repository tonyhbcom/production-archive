# 这个文件决定「哪些文件不进 Comfy Registry 的发布包」。
# 语法跟 .gitignore 一样，在它基础上再叠一层 ——
# 也就是说：这里的文件仍然留在 GitHub 仓库，但用户从 Manager 安装时不会下到本地。
#
# 判断标准：一个文件对「已经装好插件的用户」有用吗？没用就别进包。

# 截图是给 GitHub 上读 README 的人看的，装到本地用不上 —— 省 1MB 下载
screenshots/

# 开发用的矢量图标源文件，运行时只读 icon.png
icon.svg
